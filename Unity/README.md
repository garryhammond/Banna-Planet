# Unity export area

Unity-ready assets, scripts, scenes, and setup notes will live here when the first playable prototype is defined.

Do not commit Unity-generated folders such as `Library` or `Temp`; the repository's `.gitignore` already excludes them.
